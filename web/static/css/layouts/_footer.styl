footer
  height: 150px
  padding: 40px
  +mobile()
    height: 200px
    padding: 10px
  color: $background
  background: linear-gradient(top, $steelblue, gray)
  .left-col
    col(1/2)
    listnone()
    a
      link(@color, true)
  .right-col
    col(1/2)
    listnone()
    a
      link(@color, true)

.push
  height: 150px
