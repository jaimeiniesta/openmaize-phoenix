footer
  min-height: 200px
  margin-top: 40px
  padding: 40px 0
  color: $offwhite
  background: linear-gradient(top, $steelblue, gray)
  .left-col
    col(1/3)
    listnone()
    a
      link(@color, true)
  .mid-col
    col(1/3)
    listnone()
    a
      link(@color, true)
  .right-col
    col(1/3)
    listnone()
    a
      link(@color, true)
