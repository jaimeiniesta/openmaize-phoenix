@require 'nib'
@require 'jeet'
@require 'rupture'

#root
  center()
  .aside
    col(1/4)
    ul
      list-style none
      li
        display block
        margin 0 0 10px 0
    +below(767px)
      stack()
      ul
        center()
        li
          display inline
          margin 0 15px

  .article
    col(3/4)
    +below(767px)
      stack()
