@require 'nib'
@require 'jeet'
@require 'rupture'
@require '_variables'
@require './components/*'
@require './layouts/*'

body
  background-color: $offwhite

.container
  padding: 0 20px

a
  color: blue
  text-decoration: none
  &:hover
    color: lighten(@color, 30%)

.btn
  glossy-button()
  click-shrink()
.btn-danger
  glossy-button(red, "small")

.text-right
  text-align: right
