@require 'buttons'
@require 'utils'
@require 'vars'

body
  background-color: $offwhite

.container
  padding: 0 20px

a
  color: blue
  text-decoration: none
  &:hover
    color: lighten(@color, 30%)

.btn
  glossy-button()
  click-shrink()
  rounded(12px)
